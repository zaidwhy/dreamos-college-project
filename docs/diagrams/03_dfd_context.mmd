flowchart LR
    USER(("User"))
    DREAMOS["0\nDreamOS System"]
    OLLAMA[("Ollama\nLocal LLM Runtime")]
    VAULT[("File Vault")]

    USER -->|"natural language request,\nclicks, workspace actions"| DREAMOS
    DREAMOS -->|"search results, related files,\nworkspace suggestions, opened files,\norganize summaries"| USER
    DREAMOS -->|"embed / generate requests"| OLLAMA
    OLLAMA -->|"embeddings / JSON completions"| DREAMOS
    DREAMOS -->|"read + move files"| VAULT
    VAULT -->|"file contents"| DREAMOS
